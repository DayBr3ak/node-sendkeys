'use strict'
// const { default: sendKeys } = require('./sendKeys')

describe('happy path (integration)', () => {
  it('is happy', () => {
    // expect(() => sendKeys('foobar')).not.toThrow()
  })
})
